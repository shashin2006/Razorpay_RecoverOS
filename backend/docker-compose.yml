services:
  postgres:
    image: postgres:16
    container_name: recoveryos-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: recoveryos
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: recoveryos_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: